# Sainsbury's Code Test

## Getting Prepared

You need libxml & curl extensions, for DOM parsing & HTTP requests.

Do `composer install` to get all the requisites.

## Running the tests

`bin/phpunit test/`

## Running the app

`php app/app.php`
